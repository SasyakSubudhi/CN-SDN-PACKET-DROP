# CN-SDN-PACKET-DROP
simulates packet dropping using SDN. The POX controller installs a DROP rule so that all traffic from h1 (10.0.0.1) to h3 (10.0.0.3) is blocked. All other traffic is allowed and forwarded normally.
